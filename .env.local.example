# API base URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=

# Tenant identifier
NEXT_PUBLIC_API_TENANT=

# Bearer token — only needed for protected/admin routes (optional for storefront)
NEXT_PUBLIC_API_TOKEN=
